name: Android CI Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    # **التعديل الهام: تثبيت Android SDK Platform 34**
    - name: Install Android SDK Platform 34
      run: |
        # يتم استخدام sdkmanager لتثبيت حزمة منصة Android 34
        echo "y" | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "platforms;android-34"
        
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      
    - name: Create dummy keystore for debug signing
      run: |
        keytool -genkey -v -keystore debug.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"
        mkdir -p app
        mv debug.keystore app/debug.keystore

    - name: Build debug APK
      run: ./gradlew assembleDebug

    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: StudyApp-Debug-APK
        path: app/build/outputs/apk/debug/app-debug.apk
